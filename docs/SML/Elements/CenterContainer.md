# CenterContainer

## Godot Mapping
Godot Node: CenterContainer

## Properties
(To be generated)

## Events
(To be generated)

## Example

```sml
CenterContainer {
    id: example
}
```

# CenterContainer

## Godot Mapping
Godot Node: CenterContainer

## Inheritance

CenterContainer → Container → Control → CanvasItem → Node → Object

## Properties

This element supports **all Godot properties**, including inherited ones, exactly as provided by Godot for `CenterContainer`.

The authoritative, complete list is generated from the runtime class database and lives in:

- `docs/sms-reference.sml`

> Note: This per-element doc intentionally does not duplicate the full property list to avoid drift.

## Events

This element supports **all Godot signals/events**, including inherited ones, as provided by Godot for `CenterContainer`.

The authoritative, complete list is generated from the runtime class database and lives in:

- `docs/sms-reference.sml`

## Example

```sml
CenterContainer {
    id: center

    // inherited from CanvasItem
    visible: true         // default

    // inherited from Control
    // (examples only; the full list is in docs/sms-reference.sml)
    mouseFilter: stop     // default
}
```

## SMS Event Examples

```sms
// container events are inherited; see docs/sms-reference.sml for the full list
```