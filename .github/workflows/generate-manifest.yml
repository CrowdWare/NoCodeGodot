name: Validate manifests

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "scripts/generate_manifest.py"
      - "scripts/build_manifest.sh"
  workflow_dispatch:

jobs:
  validate-manifests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Generate manifests
        run: bash scripts/build_manifest.sh

      - name: Check manifests are up to date
        run: git diff --exit-code -- docs/Default/manifest.sml docs/ForgeDesigner/manifest.sml
