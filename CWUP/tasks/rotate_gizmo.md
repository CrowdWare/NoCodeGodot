# Rotate Gizmo

Style the 3D rotation gizmo like in Blockbench (see picture).
It markd the pivot point. Shows each local rotation axis und has got a nice trapez form handle.
This is better UX that the sphere handles we got now.

![gizmo](/images/rotate_gizmo.png)


Also the blue circle lets the gizmo be seen easier and the grey circle together with the red, green and blue circle part mimic a sphere.
Display it always on top.
Also use these thin line instead of the thick lines we have right now.