# AGENTS.md

This repository uses [`AGENT.md`](/Users/art/SourceCode/Forge/AGENT.md) as the canonical agent guide.

If there is any mismatch, follow `AGENT.md`.

## Local note
- Do not run builds from the sandbox (`./run.sh build`, `dotnet build`, etc.).
- The user runs builds locally outside the sandbox.
